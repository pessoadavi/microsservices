# Arquivo lido antes do contexto (application.properties)
#Necessário para executar as informações do config server antes do hibernate

spring.application.name=fornecedor
spring.profiles.active=default
spring.cloud.config.uri=http://localhost:8888